import {Button} from '@components/Button';
import type {NextPage} from 'next';

const Home: NextPage = () => {
  return (
    <div>
      <Button>button</Button>
    </div>
  );
};

export default Home;
